import React from 'react';
import { View, Text } from 'react-native';
import styles from './style';

const Settings = ({ navigation }) => {
    return (
        <View style={styles.container}>
            <Text>Hello from settings page</Text>
        </View>
    )
}

export default Settings;